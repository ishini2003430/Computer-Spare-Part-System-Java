<%@ page import="java.sql.*" %>
<%@ page contentType="text/html; charset=UTF-8" language="java" %>
<!DOCTYPE html>
<html>
<head>
    <title>View Printers - TechParts</title>
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            background-color: #f9fafb;
            padding: 40px;
        }

        h2 {
            text-align: center;
            color: #222;
        }

        .add-button {
            display: block;
            width: fit-content;
            margin: 20px auto;
            background-color: #28a745;
            color: white;
            padding: 12px 25px;
            text-decoration: none;
            border-radius: 6px;
            font-size: 16px;
            font-weight: bold;
            text-align: center;
        }

        .add-button:hover {
            background-color: #218838;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
        }

        th, td {
            padding: 12px 15px;
            border: 1px solid #ddd;
            text-align: center;
        }

        th {
            background-color: #0066cc;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }

        a.action-link {
            color: #0066cc;
            text-decoration: none;
            font-weight: bold;
        }

        a.action-link:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

    <h2>Printer List</h2>

    <a href="addprinter.jsp" class="add-button">+ Add New Printer</a>

    <table>
        <tr>
            <th>ID</th>
            <th>Model Name</th>
            <th>Brand</th>
            <th>Type</th>
            <th>Price (LKR)</th>
            <th>Cash Price (LKR)</th>
            <th>Stock Status</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>

        <%
            try {
                Class.forName("com.mysql.jdbc.Driver");
                Connection con = DriverManager.getConnection("jdbc:mysql://localhost:3306/computer", "root", "ishini2003");
                Statement stmt = con.createStatement();
                ResultSet rs = stmt.executeQuery("SELECT * FROM printers");

                while(rs.next()) {
        %>
            <tr>
                <td><%= rs.getInt("id") %></td>
                <td><%= rs.getString("model_name") %></td>
                <td><%= rs.getString("brand") %></td>
                <td><%= rs.getString("type") %></td>
                <td><%= rs.getDouble("price") %></td>
                <td><%= rs.getDouble("cash_price") %></td>
                <td><%= rs.getString("stock_status") %></td>
                <td><%= rs.getString("description") %></td>
                <td>
                    <a href="editprinter.jsp?id=<%= rs.getInt("id") %>" class="action-link">Edit</a> |
                    <a href="DeletePrinterServlet?id=<%= rs.getInt("id") %>" class="action-link" onclick="return confirm('Are you sure you want to delete this printer?');">Delete</a>
                </td>
            </tr>
        <%
                }
                con.close();
            } catch(Exception e) {
                out.println("<tr><td colspan='9'>Error: " + e.getMessage() + "</td></tr>");
            }
        %>

    </table>

</body>
</html>
