<%@ page contentType="text/html; charset=UTF-8" language="java" %>
<%@ page import="ComputerPackage.RegisterModel" %>
<%
    RegisterModel user = (RegisterModel) request.getAttribute("registerDetails");
    if (user == null) {
        out.println("<h2>User not found!</h2>");
        return;
    }
%>
<!DOCTYPE html>
<html>
<head>
    <title>Update User</title>
    <style>
        body {
            font-family: Arial;
            background-color: #f2f2f2;
            padding: 30px;
        }

        .form-container {
            width: 400px;
            margin: auto;
            background-color: white;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0px 0px 10px #ccc;
        }

        .form-container h2 {
            text-align: center;
            color: #333;
        }

        input[type=text], input[type=email], input[type=password] {
            width: 100%;
            padding: 10px;
            margin-top: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        button {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            width: 100%;
            border-radius: 4px;
        }

        button:hover {
            background-color: #0056b3;
        }

        a {
            display: block;
            margin-top: 10px;
            text-align: center;
            text-decoration: none;
            color: #555;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>

<div class="form-container">
    <h2>Update User</h2>
    <form action="RUpdateServlet" method="post">
        <input type="hidden" name="id" value="<%= user.getRegisterid() %>">

        <label>Full Name:</label>
        <input type="text" name="fullname" value="<%= user.getFullname() %>" required>

        <label>Email:</label>
        <input type="email" name="email" value="<%= user.getEmail() %>" required>

        <label>Username:</label>
        <input type="text" name="username" value="<%= user.getUsername() %>" required>

        <label>Password:</label>
        <input type="password" name="password" value="<%= user.getPassword() %>" required>

        <button type="submit">Update</button>
    </form>
    <a href="registeraccount.jsp">← Back to User List</a>
</div>

</body>
</html>
